include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - ".dart_tool/**"
    - "build/**"
    - "lib/core/l10n/generated/**"

linter:
  rules:
    # avoid_print: false
